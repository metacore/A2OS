MODULE srHex;
	IMPORT srBase;
CONST 
	Z = 8.66000000000000E-1; 
	W = (1 / 2); 
	S = (2 * Z); 
	TY = (2 * Z); 
	TX = 3; 
	E = 1.00000000000000E-1; 
TYPE 
	SREAL = srBase.SREAL; 

	PROCEDURE ^ hexize*(x, y: SREAL): BOOLEAN; 
	PROCEDURE ^ hexize2*(x, y: SREAL): SREAL; 
BEGIN
END srHex.
