MODULE vyChiSquare IN Oberon;
	IMPORT MathL IN A2;
CONST 
	INF = MAX(FLOAT64); 
TYPE 
	extended = FLOAT64; 

	integer = SIGNED32; 

	PROCEDURE ^ SChiSq*(n: integer; x: extended): extended; 
	PROCEDURE ^ PFChiSq*(n: integer; x: extended): extended; 
	PROCEDURE ^ chs1*(bins: ARRAY OF SIGNED32; ebins: ARRAY OF FLOAT64; nbins: SIGNED32; knstrn: SIGNED32; VAR df, chsq, prob: FLOAT64); 
BEGIN
END vyChiSquare.
