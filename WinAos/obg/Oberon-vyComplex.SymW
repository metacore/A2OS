MODULE vyComplex IN Oberon;
	IMPORT Math IN A2;
CONST 
	idversion* = \" v0r0d1"\; 
TYPE 
	real = REAL; 

	index = LONGINT; 

	PROCEDURE ^ PolarToC*(len: index; VAR rho, phi: ARRAY OF real; VAR Re, Im: ARRAY OF real; start: index); 
BEGIN
END vyComplex.
