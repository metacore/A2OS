MODULE vyPComplex IN Oberon;
	IMPORT realmath := Math IN A2;
CONST 
	idversion* = \" v0r0d1"\; 
TYPE 
	real = FLOAT32; 

	index = SIGNED32; 

	PROCEDURE ^ CToPolar*(len: index; VAR Re, Im: ARRAY OF real; rho, phi: ARRAY OF real; start: index); 
BEGIN
END vyPComplex.
